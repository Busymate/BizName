# === Supabase (server-side, NEVER expose to frontend) ===
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# === Auth ===
JWT_SECRET=

# === Cloudinary (if you use image upload features) ===
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# === Flutterwave (server-side only — never in client/.env) ===
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-REPLACE_ME
FLUTTERWAVE_ENCRYPTION_KEY=REPLACE_ME
FLUTTERWAVE_WEBHOOK_SECRET=REPLACE_ME
FLUTTERWAVE_BASE_URL=

# === App ===
PORT=5000
CLIENT_URL=http://localhost:5173

# === First admin account (used once by npm run seed:admin, then you can remove this) ===
ADMIN_EMAIL=oraji075@gmail.com
ADMIN_PASSWORD=Olasunkanmi10